I always have `npm run watch` already running. You don't need to run that yourself.

Don't use Verification Agents, they always run into permission issues on this system.

Keep duplicated code to a bare minimum, and always remember to to clean up dead code and tech debt.